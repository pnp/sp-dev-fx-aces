name: External validation

on:
  pull_request:
    branches:
      - "main"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate
        uses: Balastrong/validate-sample-action@main
        with:
          gh-token: ${{ secrets.GITHUB_TOKEN }}
